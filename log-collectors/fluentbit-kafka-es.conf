[INPUT]
    Name tail
    Path /var/log/containers/*.log
    Parser cri

[OUTPUT]
    Name kafka
    Match *
    Brokers kafka:9092
    Topic logs

# Then separate Fluent Bit for Kafka → ES
[INPUT]
    Name kafka
    Brokers kafka:9092  
    Topics logs

[OUTPUT]
    Name es
    Match *
    Host elasticsearch:9200